# WebSocket Host Configuration
# Set this to your WebSocket server host (without protocol)
# Default: run-coops-767192.apps.shift.nerc.mghpcc.org
VITE_WS_HOST=run-coops-767192.apps.shift.nerc.mghpcc.org

# WebSocket Protocol Configuration (optional)
# Force a specific WebSocket protocol (ws: or wss:)
# If not set, it will auto-detect based on page protocol (https->wss, http->ws)
# Useful for development when WebSocket server doesn't support wss://
# VITE_WS_PROTOCOL=ws:

# Google Maps API Key
# Required for displaying Google Maps in the GPS Location panel
# Get your API key from: https://console.cloud.google.com/
# Make sure to enable "Maps JavaScript API" and "Static Maps API"
VITE_GOOGLE_MAPS_API_KEY=AIzaSyALLA5zchIEEUzAJ9sUxqw7B_jo79B1z58